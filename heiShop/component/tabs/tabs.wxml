
<view class="tabs">
    <view class="tabs_title">
        <view bindtap="handleTabs" data-index="{{index}}" class="tabs_item {{item.isActive?'active':''}}"  wx:for="{{tabs}}" wx:key="index">{{item.value}}</view>
    </view>
    <view class="tabs_content">
        <slot></slot>
    </view>
</view>